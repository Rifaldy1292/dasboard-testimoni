{
  "name": "dashboard-machine",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "client": "cd client && pnpm run dev",
    "server": "cd server && pnpm run start",
    "pull": "cd client && git pull origin main && pnpm run build-only",
    "backup-db": "cd server/backup && \"C:\\Program Files\\PostgreSQL\\17\\bin\\pg_dump.exe\" -U postgres -h localhost -p 5432 -F c -b -v -f dashboard_machine_backup.dump yamaha_dashboard_machine",
    "push-db": "pnpm run backup-db && git add . && git commit -m \"backup db\" && git push origin main",
    "restore-db": "cd server && pnpm run full-restore",
    "commit": "git-cz",
    "install:all": "pnpm install && cd client && pnpm install && cd ../server && pnpm install"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jonfry1175/dashboard-machine.git"
  },
  "config": {
    "commitizen": {
      "path": "@commitlint/cz-commitlint"
    }
  }
}
